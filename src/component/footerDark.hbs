{{#with footer}}
        <div class="footer_a">
            {{#each pages}}
            <a href="{{path}}" class="footer_links">{{name}}</a>
        {{/each}}
        </div>
    <div>
        <span class="footer_copyright">{{copyright}}</span>
    </div>
{{/with}}